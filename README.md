- Calculates quantitative features including momentum, volatility, and relative volume from several years of
  historical stock data
- Uses matrix calculus to represent and manipulate these features across many days
- Predicts next day stock returns using a multivariate regression model 
  whose Weights of the model are trained using the gradient descent algorithm with a fixed learning rate
